"run pip using this specific Python interpreter".
"Navigate to the python.exe forlder".
"Make sure to use CMD".

python -m pip install openai

python -m pip install python-dotenv

python -m pip install aiohttp discord.py

python -m pip install langgraph langchain-openai langchain-core

python -m pip install asyncpg

---------------- Postgre SQL --------------
sudo apt update
sudo apt install postgresql postgresql-contrib

#Start and enable PostgreSQL:
sudo systemctl start postgresql
sudo systemctl enable postgresql

#Check status:
sudo systemctl status postgresql

#Log in as the postgres OS user
sudo -u postgres psql

#You should now see a prompt like:
postgres=#

#Inside psql, run:
\password postgres

#It will ask:
Enter new password:
Enter it again:

#Type the password you want (e.g. 1988), then exit:
\q


-------------------------
ctrl + H to see hidden files
-------
 "To run on Linux"

# 1. Make sure you have Python and pip
sudo apt update
sudo apt install -y python3 python3-pip

# 2. Ensure ~/.local/bin is on your PATH (once)
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

cd ~/Desktop/Python
python3 -m venv .venv
--> To check ls -la
should show .venv Fail
--> run the script:
source .venv/bin/activate
shoud appear: 9.venv
--> run the scripts 
python -m pip install --upgrade pip
pip install -r requirements.txt

# 4. Run your bot
python3 AgenticAI.py

